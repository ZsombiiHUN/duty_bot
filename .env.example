DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
DISCORD_TOKEN="DISCORD_BOT_TOKEN"
CLIENT_ID="CLIENT_ID"

# Optional: Specify which Discord server IDs the bot is allowed to operate on.
# Leave empty or set to "*" to allow all servers.
# Use comma-separated values for multiple specific servers (e.g., "12345,67890").
ALLOWED_SERVER_IDS=""

# Required: The ID of the role used to signify a user is "on duty".
DUTY_ROLE_ID="YOUR_DUTY_ROLE_ID"
